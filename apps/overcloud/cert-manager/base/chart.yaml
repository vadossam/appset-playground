apiVersion: builtin
kind: HelmChartInflationGenerator
metadata:
  name: cert-manager

name: cert-manager
repo: https://charts.jetstack.io
version: 1.7.0

releaseName: cert-manager

valuesInline:
  installCRDs: true
